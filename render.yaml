services:
  - type: web
    name: unientry-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: unientry_jwt_secret_2024_secure
      - key: PORT
        value: 5000
